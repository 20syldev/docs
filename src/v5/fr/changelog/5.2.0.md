# 5.2.0

## API

**[API v5.2.0](https://github.com/20syldev/api/releases/tag/5.2.0)** — Asymmetric.

- Ajout de l'endpoint **[POST /asymmetric](/v5/fr/asymmetric)** : génère des paires de clés RSA et effectue le chiffrement et déchiffrement asymétrique sans dépendance externe. Supporte deux algorithmes OAEP : `rsa-oaep-sha256` (défaut) et `rsa-oaep-sha1`. Trois actions sont disponibles : `keygen` génère une paire de clés publique/privée encodées en PEM (2048 ou 4096 bits), `encrypt` chiffre un texte avec la clé publique et retourne le résultat en base64, et `decrypt` retrouve le texte original à partir de la clé privée. La taille maximale du texte dépend de la taille de la clé et de l'algorithme en raison du padding OAEP.
- Suppression du tableau des endpoints du README au profit d'un lien direct vers la documentation.

## Documentation

- Ajout de la page de documentation pour **[Asymmetric](/v5/fr/asymmetric)** : couvre les trois actions (`keygen`, `encrypt`, `decrypt`), les paramètres conditionnels (`text`, `publicKey`, `privateKey`), un tableau des limites de taille avec la formule OAEP pour chaque combinaison de clé et d'algorithme, et des sections de champs de réponse séparées pour `keygen` et `encrypt`/`decrypt`.
- Ajout de l'endpoint dans le sidebar et le registre Playground.
- Le Playground filtre désormais les endpoints selon le sidebar de la version active, masquant les routes non disponibles.
- Extraction d'une regex `VERSION_PATH` partagée dans `redirect.ts`, réutilisée dans `Version.vue` et `Playground.vue`.
- La couleur du lien home dans la navbar utilise maintenant une classe `.home` au lieu de sélecteurs d'attribut `[href]` codés en dur.
- Suppression du `collapsed: false` redondant sur tous les groupes `Endpoints` du sidebar (comportement par défaut).
- Correction de l'alignement des colonnes du tableau des actions OTP.
